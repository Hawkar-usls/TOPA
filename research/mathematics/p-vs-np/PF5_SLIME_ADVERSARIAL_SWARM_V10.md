# PF5 Slime Adversarial Swarm v10

Status: **FROZEN BEFORE PROVIDER RUN**  
Claim ceiling: **P_VS_NP = OPEN**

## Purpose

Attack the current Slime semantic-pressure candidate instead of accumulating favorable random tests.

PF5 v9.1 showed that the frozen Slime order beat the three original non-Slime baselines on eight connected-3CNF holdouts. That is useful finite evidence, but those baselines were narrow. v10 therefore introduces a deliberately dumb but broader **fixed deterministic random-order swarm**. The swarm is not allowed to use PS-width, SAT, branch assignments, witnesses, or probe outcomes during generation.

If even one random challenger beats Slime, that is preserved as a candidate-discovery counterexample. It does not imply hardness. If Slime beats all challengers, that is still only finite portfolio evidence.

## Frozen source family

Connected signed 3-CNF generated by the unchanged v9 constructor:

- variables: `n = 7`
- clauses: `m = 10`
- seeds, frozen now before provider execution:

`[906500, 906501, 906502, 906503, 906504, 906505, 906506, 906507, 906508, 906509, 906510, 906511, 906512, 906513, 906514, 906515, 906516, 906517, 906518, 906519, 906520, 906521, 906522, 906523, 906524, 906525, 906526, 906527, 906528, 906529, 906530, 906531]`

The Slime producer remains pinned to:

`Hawkar-usls/Janus-Demiurge@36437e053bb151660eaed4e82634d430346343a5`

## Frozen challenger portfolio

For each source:

1. generate the complete Slime manifest first;
2. extract the frozen `SLIME_SEMANTIC_PRESSURE` order;
3. generate exactly `32` deterministic random permutations of the same incidence leaves;
4. challenger seed is derived only from `(domain tag, source SHA-256, challenger index)`;
5. freeze and hash all 33 candidate orders before any exact PS probe.

No challenger may be added, deleted, repaired or regenerated after scoring begins.

The random portfolio is intentionally simple. Its only scientific role is to test whether a single greedy Slime front is unnecessarily brittle.

## Exact finite judge

Reuse the independent v9 C032/STV exact caterpillar PS-signature scorer.

For every candidate order, score all `2r-3` caterpillar edge cuts and record:

`max_cut max(|PS_left|, |PS_right|)`.

The exact scorer is exponential and remains an **audit oracle only**, not a polynomial SAT routine.

## Frozen questions

- On how many of the 32 sources is Slime strictly beaten by at least one of the 32 random challengers?
- What is the maximum loss ratio `SlimeWidth / BestRandomWidth`?
- What is Slime's tie-aware rank among the 33 frozen candidates?
- Do losses cluster around source-only observables that can be promoted into a proof-carrying feature without consulting exact PS results at runtime?
- What did the broader portfolio cost to generate and verify?

## Interpretation discipline

A Slime loss means only:

`CURRENT_SINGLE_FRONT_SLIME_ROUTER_NOT_PORTFOLIO_COMPLETE`.

It licenses a next candidate-generator design such as deterministic multi-front / multi-temperature Slime, but only with a fresh holdout after the new generator is frozen.

A Slime win means only:

`NO_ESCAPE_FOUND_IN_THIS_FIXED_32_CHALLENGER_FINITE_PORTFOLIO`.

Neither terminal resolves P versus NP.

## Prohibitions

- no post-score hyperparameter tuning inside v10;
- no replacing the frozen source seeds;
- no selecting random challengers based on exact score;
- no exact PS signal inside Slime trace update;
- no SAT oracle;
- no interpreting random success as theorem authority;
- no interpreting Slime failure as a lower bound;
- no `P=NP` claim.

## Next gate if losses are found

`SLIME_MULTI_FRONT_PROOF_CARRYING_CANDIDATE_SWARM`

The new producer must emit a polynomial-sized family of assignment-independent candidates **before** a new blind holdout and charge all generation work.

`P_VS_NP = OPEN`
