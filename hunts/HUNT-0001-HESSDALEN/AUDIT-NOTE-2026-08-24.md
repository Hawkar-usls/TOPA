# Audit note — 2026-08-24

The second spiral deliberately stops short of claiming that the raw-day replay has run. Public documentation confirms that a full raw day exists for testing, but the current retrieval path did not expose the directory listing needed for deterministic enumeration/download.

This is recorded as an acquisition gate, not hidden as a tooling inconvenience.

At the same time, the planned two-camera parallax step was corrected because the two currently operational 4K cameras are documented on the same Blue Box mast. Coverage diversity is not stereo baseline.
