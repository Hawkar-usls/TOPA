name: Validate Akinator U1I3 Compression Block Search

on:
  push:
    paths:
      - 'research/mathematics/p-vs-np/C025_AKINATOR_U1I3_COMPRESSION_BLOCK_SEARCH_BARRIER.md'
      - 'research/mathematics/p-vs-np/akinator_u1i3_compression_block_probe.py'
      - '.github/workflows/validate-akinator-u1i3-block-search.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  finite-mechanics:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run compression-block search finite mechanics
        run: python research/mathematics/p-vs-np/akinator_u1i3_compression_block_probe.py
      - name: Claim ceiling reminder
        run: |
          echo 'CI checks finite schema-count mechanics only.'
          echo 'This closes brute-force block enumeration, not all constructive compressors.'
          echo 'Direct constructive projection grammar remains OPEN.'
          echo 'P_VS_NP = OPEN'
