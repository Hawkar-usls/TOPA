name: Validate TOPA SPIDER Relation Pull v2

on:
  push:
    paths:
      - 'tools/topa_spider_v2.py'
      - 'tools/topa_spider_v21.py'
      - 'tools/topa_archive_gateway.py'
      - 'protocols/TOPA_SPIDER_RELATION_DRIVEN_PULL_PROTOCOL_v2.0.json'
      - 'protocols/TOPA_SPIDER_RELATION_DRIVEN_PULL_PROTOCOL_v2.1.json'
      - 'raw/unexamined/archive/bootstrap-2026-08-27/archive-bootstrap-seeds.jsonl'
      - '.github/workflows/spider-relation-pull-v2.yml'
  workflow_dispatch:

# LIVE_SEARCH_MARKER: 2026-08-27T16:58:00+03:00
# V2.1 correction: collection-wide tags are IDF-attenuated and cannot pull alone.
# Hypothesis-agnostic run: no topic query steering is supplied.

jobs:
  selective-spider:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Self-test SPIDER v2.1
        run: python tools/topa_spider_v21.py self-test
      - name: Broad official archive survey stays ephemeral
        run: |
          mkdir -p work/spider-v21
          python tools/topa_archive_gateway.py ingest \
            --providers nara,cia,nsa,fbi \
            --limit 100 \
            --expand-nara \
            --out work/spider-v21/archive-survey.jsonl \
            --receipt work/spider-v21/archive-survey-receipt.json
      - name: Pull only informatively connected records
        run: |
          python tools/topa_spider_v21.py pull \
            --seed raw/unexamined/archive/bootstrap-2026-08-27/archive-bootstrap-seeds.jsonl \
            --survey work/spider-v21/archive-survey.jsonl \
            --out work/spider-v21/spider-pulled.jsonl \
            --receipt work/spider-v21/spider-pull-receipt.json \
            --threshold 0.24 \
            --semantic-only-threshold 0.42 \
            --max-pull 100 \
            --rounds 3
      - name: Weave prior web plus selectively pulled records
        run: |
          cat raw/unexamined/archive/bootstrap-2026-08-27/archive-bootstrap-seeds.jsonl \
              work/spider-v21/spider-pulled.jsonl > work/spider-v21/current-web.jsonl
          python tools/topa_spider_v21.py weave \
            --input work/spider-v21/current-web.jsonl \
            --nodes work/spider-v21/spider-nodes.jsonl \
            --edges work/spider-v21/spider-edges.jsonl \
            --receipt work/spider-v21/spider-weave-receipt.json \
            --semantic-threshold 0.14 \
            --topk 8 \
            --keyword-nodes 12
      - name: Scientific firewall assertions
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=json.loads(Path('work/spider-v21/spider-pull-receipt.json').read_text())
          w=json.loads(Path('work/spider-v21/spider-weave-receipt.json').read_text())
          proto=json.loads(Path('protocols/TOPA_SPIDER_RELATION_DRIVEN_PULL_PROTOCOL_v2.1.json').read_text())
          assert p['status']=='PASS'
          assert p['survey_candidates'] >= p['pulled_documents']
          assert p.get('query') is None
          assert p['confound_control']['generic_collection_membership_alone_can_pull'] is False
          assert 'COLLECTION_MEMBERSHIP_IS_NOT_RELATIONSHIP' in p['laws']
          assert 'NO_FIXED_HYPOTHESIS_CENTER' in w['laws']
          assert proto['topic_policy']['tachyon_is_privileged'] is False
          assert proto['topic_policy']['retrocausality_is_privileged'] is False
          assert proto['confound_controls']['generic_collection_membership_alone_can_pull'] is False
          print('SPIDER_RELATION_PULL_V2_1_FIREWALL=PASS')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: topa-spider-relation-pull-v21
          path: work/spider-v21
          retention-days: 14
